<Page
    x:Class="WpfApp2.View.ThirdPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp2.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:r="clr-namespace:WpfApp2.ViewModel.ThirdPage"
    Title="ThirdPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Page.DataContext>
        <r:ThirdPageViewModel />
    </Page.DataContext>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />

        </Grid.ColumnDefinitions>
        <TextBlock
            Text="{Binding Text}"
            Grid.Row="2"
            Grid.Column="1"
            Background="Aquamarine"
            FontSize="25"
            Foreground="Black"
            TextWrapping="Wrap" />
        <TextBox
            Text="{Binding Text}"
            FontSize="25"
            Grid.Row="0" 
            Grid.Column="1" />
        <Button
            Grid.Row="1"
            Grid.Column="1"
            Command="{Binding HelloCommand}" />

    </Grid>
</Page>
